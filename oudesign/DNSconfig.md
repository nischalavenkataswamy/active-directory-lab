# DNS Configuration

The Domain Controller hosts the DNS service.

Primary DNS:

192.168.56.10

The Windows 10 client uses the Domain Controller for DNS resolution and successfully resolves the domain.
